# Provider Configuration Status

This file intentionally does not store secrets.

Configured now:
- Copilot credential pool exists.
- Gemini credential pool exists and Gemini API was verified.
- Kimi credential exists as `kimi-coding` and config provider `kimi` exists.

Needs confirmation/configuration:
- Exact Copilot availability for `Opus 4.7`.
- Exact Kimi model availability for `Kimi K2.6`.
- Z.ai / GLM credential for Agent 5 (`GLM_API_KEY` or provider-specific Hermes config).

Do not echo API keys in logs or reports.
