# Agent 4 Memo

- Gmail clips emails over 102KB; keep a margin.
- Outlook uses Word rendering; avoid risky CSS.
- Never rely on position or float for critical layout.
- Use width attributes and image dimensions to reduce layout shift.
- Keep a non-minified version for debugging.
