# N8N Workflow Prompt — גלעוז אלקטרוניקה | Encoder Article Generator
# Agent: Hermes Opus 4.7 Sub-Agent | OpenRouter model: anthropic/claude-opus-4.7 | Run: 2026-04-27-five-openrouter-agents

## System identity
You are Hermes Opus 4.7 Sub-Agent, an expert Hebrew RTL WordPress HTML template generator for גלעוז אלקטרוניקה (galoz.co.il).

## Iron rule — WordPress 1:1 rendering
Return a FULL WordPress-ready HTML article template that renders in the WordPress HTML editor exactly like the preview:
- Hebrew RTL: lang="he" and/or dir="rtl" on the main wrapper.
- Inline CSS on all important blocks; do not depend on external CSS except the Heebo font link.
- Use full article content, not summaries and not placeholders.
- Use real images only. Empty src, fake image URLs, lorem ipsum, markdown fences, and placeholder sections are forbidden.
- Preserve source-traceable Galoz links, logo, category URL, contact URL and WhatsApp URL.
- Mobile/tablet/desktop responsive with fluid widths and no horizontal overflow.
- Safe for WordPress: no script tags, no body tag dependency, no fixed overlays that conflict with theme header.

## Source topic
Source URL: https://mapril.websmail.net/industrial-rotary-encoder-sensors-2/
Target domain: https://www.galoz.co.il/
Topic: אנקודר סיבובי / אנקודרים סיבוביים לתעשייה
Focus style: premium industrial design / executive clarity

## Required output fields for automation
When used inside an AI node, return JSON with:
- title
- wordpress_html
- seo_title
- meta_description
- images_used
- internal_links
- faq
- schema_json_ld

## Validation before final answer
Check: one H1, full content, images present, RTL, inline CSS, no placeholders, no markdown fences.


## OpenRouter agent review notes
{
  "agent_angle": "Hermes Opus 4.7 Sub-Agent",
  "raw_response": "{\"agent_angle\":\"Hermes Opus 4.7 – WordPress rendering fidelity & RTL integrity auditor for Galoz encoder guide; ensures 1:1 paste-into-WP render with zero placeholders, real CDN images, and inline-only CSS surviving Gutenberg/Classic editor sanitization.\",\"wordpress_critical_checks\":[\"Remove <!DOCTYPE>, <html>, <head>, <body> tags before pasting into WP post body – keep only <article> downward, since WP wraps content in its own document.\",\"Google Fonts <link> in <head> will be stripped by WP – either enqueue Heebo via functions.php/theme, or rely on site default Hebrew font; do NOT rely on inline <link> surviving.\",\"Confirm every style is inline on the element (no <style> blocks) because Gutenberg's HTML block preserves inline style attributes but strips <style>.\",\"Verify dir=\\\"rtl\\\" and lang=\\\"he\\\" attributes are preserved on <article> and key sections – WP auto-p / wpautop may inject <br> inside RTL text; wrap article in raw HTML block or disable wpautop for this post.\",\"Check that clamp(), CSS grid (grid-template-columns:repeat(auto-fit,minmax())), flex-wrap, and linear-gradient inline styles render in target WP theme without conflict from theme's .entry-content max-width.\",\"Theme may override max-width:1120px on article – add !important sparingly or confirm theme allows full-width blocks.\",\"<details>/<summary> TOC: some themes inject CSS that hides summary marker; list-style:none already set – verify on live theme.\",\"All anchor IDs (#encoder-role, #how-it-works, etc.) must not collide with theme IDs; prefix with galoz- if collision risk.\",\"External images from d3m9l0v76dty0.cloudfront.net must be whitelisted / not blocked by any image proxy or lazy-load plugin that rewrites src.\",\"Add loading=\\\"lazy\\\" and decoding=\\\"async\\\" to non-hero <img> to match WP core behavior and avoid double-lazy from plugins.\",\"WhatsApp & contact CTAs use absolute URLs – confirm rel=\\\"noopener\\\" on target=_blank (already present on wa.me); add rel=\\\"noopener\\\" also where missing.\",\"Ensure no smart quotes / curly quotes introduced by WP editor – paste via Code Editor / HTML block only, never Visual tab.\"],\"html_improvement_notes\":[\"Excerpt is truncated mid-sentence ('במער...') – final template MUST include full sections: how-it-works, incremental-absolute, selection, mistakes, parameters table, galoz-support, faq, plus closing </article>.\",\"Add explicit width/height attributes to all <img> to prevent CLS and satisfy WP core image handling.\",\"Logo <img> lacks width/height – add width=\\\"180\\\" height=\\\"auto\\\" equivalent (use real intrinsic dims).\",\"Add alt text to every image with descriptive Hebrew (encoder type, application) – no empty alts, no 'image' placeholders.\",\"Parameters section (#parameters) should be a real <table> with inline-styled <thead>/<tbody>/<th>/<td>, RTL-aligned, zebra rows via inline background on alternating <tr>.\",\"FAQ section should use <details>/<summary> per question OR schema-ready <h3>+<p> pairs; add FAQPage JSON-LD in a <script type=\\\"appl",
  "wordpress_critical_checks": [
    "manual parse fallback"
  ],
  "html_improvement_notes": [],
  "n8n_workflow_notes": [],
  "prompt_notes": [],
  "qa_acceptance": []
}
