---
phase: 03-template-foundation-article-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html
autonomous: true
requirements:
  - TMPL-01
  - TMPL-02
  - TMPL-03
  - TMPL-07
  - TMPL-08

must_haves:
  truths:
    - "Article is wrapped in <article dir='rtl' lang='he'> with no content outside the wrapper"
    - "All CSS is inline style attributes — zero style blocks, zero class selectors"
    - "No external CSS/JS dependencies, no CDN links exist in the file"
    - "No HTML comments, no markdown, no code fences exist in the file"
    - "The file is a valid HTML fragment starting with <article> and ending with </article>"
  artifacts:
    - path: "claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html"
      provides: "Foundation article wrapper with RTL, inline CSS, summary card"
      min_lines: 40
      contains: "dir=\"rtl\" lang=\"he\""
  key_links:
    - from: "article wrapper"
      to: "all inner elements"
      via: "inline style attributes only"
      pattern: "style=\""
---

<objective>
Create the foundation HTML template file with article wrapper, RTL Hebrew setup, inline-only CSS architecture, and "In This Article" summary card.

Purpose: Establishes the file and architectural foundation that all subsequent phases (4-8) build upon. Every CSS decision here — inline-only, no display property, no classes — prevents WordPress wp_kses_post() stripping.

Output: `claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html` — a valid HTML fragment ready for section content injection.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-template-foundation-article-structure/03-CONTEXT.md
@.planning/phases/01-baseline-audit-wordpress-risk-assessment/audit/05-wordpress-rendering-risk-summary.md

<interfaces>
<!-- WordPress-safe CSS properties (from Phase 1 audit): -->
<!-- SAFE: font-family, font-size, font-weight, color, background, background-color, background-image -->
<!-- SAFE: margin, padding, border, border-radius, border-color, border-width, border-style -->
<!-- SAFE: text-align, text-decoration, line-height, letter-spacing, vertical-align -->
<!-- SAFE: width, max-width, min-width, height, max-height, min-height -->
<!-- SAFE: overflow, overflow-x, overflow-y, float, clear -->
<!-- SAFE: flex-direction, align-items, justify-content, gap, flex-wrap, flex-shrink (BUT USELESS without display:flex) -->
<!-- SAFE (WP 6.1+): clamp(), calc(), min(), max() -->
<!-- SAFE (WP 5.3+): position, z-index, top, right, bottom, left -->
<!-- NOT SAFE: display (the property itself is blocked — not flex/grid, the display property entirely) -->
<!-- NOT SAFE: class selectors (wp_kses strips in some contexts) -->
<!-- NOT SAFE: style blocks (stripped entirely by wp_kses_post) -->

<!-- Brand data from hipsterstyle-discovery.json: -->
<!-- brand.name_he: היפסטר -->
<!-- brand.site: https://hipsterstyle.co.il/ -->
<!-- accent color (from user decision): #c8a97e -->
<!-- "In This Article" card: bg #f9f6f1, border #e8e0d4, padding 16px -->
<!-- Section divider: 1px border-top #e8e0d4 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create article wrapper with RTL setup and inline CSS architecture</name>
  <files>claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html</files>
  <read_first>
    - .planning/phases/03-template-foundation-article-structure/03-CONTEXT.md
    - .planning/phases/01-baseline-audit-wordpress-risk-assessment/audit/05-wordpress-rendering-risk-summary.md
  </read_first>
  <action>
Create the directory `claude-code/Files/` if it does not exist.

Create the HTML template file as a valid HTML fragment (NOT a full HTML page — no doctype, no html/head/body tags). The entire content lives inside one `<article>` wrapper.

Article wrapper element:
```html
<article id="hs-top" dir="rtl" lang="he" style="max-width:780px;margin:0 auto;padding:0 clamp(14px,4vw,28px) 60px;font-family:'Arial','Helvetica Neue',sans-serif;color:#2d2d2d;line-height:1.85;background:#ffffff">
```

Key architecture rules (per CONTEXT.md decisions and Phase 1 audit):
- `max-width:780px` (per D: max article width 780px)
- `font-family:'Arial','Helvetica Neue',sans-serif` (per D: safe in all WP themes)
- `color:#2d2d2d` (per D: body text color)
- NO `box-sizing:border-box` (uncertain WP safety per L1 audit finding — use padding calculations that work without it)
- NO `display` property anywhere (per audit: NOT on WP safe_style_css whitelist)
- NO class attributes anywhere (per audit: dead weight, H1 finding)
- NO style blocks (per TMPL-03, CRIT-1 audit)
- NO HTML comments (per TMPL-08)
- NO external CSS/JS/CDN links (per TMPL-07)

Inside the article wrapper, add:
1. A 2-paragraph intro placeholder area with:
   - Each `<p>` styled: `style="margin:0 0 1.4rem;font-size:clamp(16px,2.5vw,19px);line-height:1.85;color:#2d2d2d;text-align:right"`
   - Placeholder Hebrew text: first paragraph about the article topic (kids fashion tips), second paragraph bridging to the content
   - An anchor link example styled with: `style="color:#c8a97e;text-decoration:underline"` (accent color per decision)

2. An "In This Article" summary card (the "במאמר זה" section):
```html
<section style="background:#f9f6f1;border:1px solid #e8e0d4;border-right:4px solid #c8a97e;border-radius:12px;padding:16px;margin:0 0 2rem">
  <p style="margin:0 0 0.8rem;font-size:clamp(16px,2.5vw,19px);font-weight:700;color:#1a1a1a;text-align:right">במאמר זה</p>
  <ul style="margin:0;padding:0 1.2rem 0 0;color:#2d2d2d;line-height:1.95;text-align:right">
    <li style="margin:0 0 0.4rem">placeholder summary item 1</li>
    <li style="margin:0 0 0.4rem">placeholder summary item 2</li>
    <li style="margin:0 0 0.4rem">placeholder summary item 3</li>
    <li style="margin:0 0 0.4rem">placeholder summary item 4</li>
    <li style="margin:0">placeholder summary item 5</li>
  </ul>
</section>
```

3. Close with `</article>` — nothing outside the article wrapper.

CRITICAL: Every single element must have `text-align:right` explicitly set on its inline style (per audit L3 — guard against WP theme CSS overriding inherited RTL).
CRITICAL: Zero occurrences of `display:` anywhere (per audit: display property stripped by wp_kses_post).
CRITICAL: Zero occurrences of `class=` anywhere (per audit H1: dead weight without style block).
  </action>
  <verify>
    <automated>bash -c "FILE='claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html' && test -f \"$FILE\" && echo 'FILE EXISTS' && head -1 \"$FILE\" | grep -q 'dir=\"rtl\"' && echo 'RTL OK' && ! grep -q '<style' \"$FILE\" && echo 'NO STYLE BLOCK' && ! grep -q 'class=' \"$FILE\" && echo 'NO CLASSES' && ! grep -q '<!--' \"$FILE\" && echo 'NO COMMENTS' && ! grep -qi 'display:' \"$FILE\" && echo 'NO DISPLAY' && echo 'ALL CHECKS PASSED'"</automated>
  </verify>
  <acceptance_criteria>
    - grep 'dir="rtl" lang="he"' returns match on article tag
    - grep '<style' returns zero matches
    - grep 'class=' returns zero matches
    - grep '<!--' returns zero matches
    - grep -i 'display:' returns zero matches
    - grep 'max-width:780px' returns match
    - grep '#c8a97e' returns at least 1 match (accent color)
    - grep '#f9f6f1' returns at least 1 match (summary card bg)
    - grep 'text-align:right' returns matches on text elements
    - File starts with '<article' and ends with '</article>'
  </acceptance_criteria>
  <done>
    - HTML file exists at claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html
    - Article wrapper has dir="rtl" lang="he" with max-width:780px
    - Zero style blocks, zero class attributes, zero HTML comments, zero display properties
    - "In This Article" summary card present with correct colors (#f9f6f1 bg, #e8e0d4 border, #c8a97e accent)
    - All text elements have explicit text-align:right
    - File is a valid HTML fragment (no doctype/html/head/body)
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate WordPress safety and file integrity</name>
  <files>claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html</files>
  <read_first>
    - claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html
  </read_first>
  <action>
Run a comprehensive validation sweep on the created file and fix any violations found:

1. Verify NO forbidden patterns exist:
   - `display:` (any form — flex, grid, block, inline-block, none)
   - `class=` attributes
   - `<style` blocks
   - `<!--` HTML comments
   - `<link` or `<script src=` external dependencies
   - `cdn.` or `googleapis.com` or `cloudflare` CDN references
   - ` ``` ` code fences or `#` markdown headings outside HTML
   - `http://` (should be https if any URLs)

2. Verify REQUIRED patterns exist:
   - `dir="rtl"` and `lang="he"` on article tag
   - `max-width:780px` on article
   - `font-family:'Arial','Helvetica Neue',sans-serif` or close variant
   - `color:#2d2d2d` (body text)
   - `color:#1a1a1a` (heading text)
   - `#c8a97e` (accent color, at least once)
   - `text-align:right` on every `<p>`, `<li>`, `<ul>`, `<section>` that contains text
   - `clamp(` used for font-size (responsive typography)
   - File first non-empty line starts with `<article`
   - File last non-empty line is `</article>`

3. If any violation found, fix it in place and re-run the checks.

4. Count total lines — should be between 30 and 80 for the foundation (before section stubs are added in Plan 02).
  </action>
  <verify>
    <automated>bash -c "FILE='claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html' && echo '=== FORBIDDEN ===' && for pat in 'display:' 'class=' '<style' '<!--' '<link ' '<script src' 'cdn\\.' 'googleapis' 'cloudflare'; do c=$(grep -ci \"$pat\" \"$FILE\" 2>/dev/null || echo 0); echo \"$pat: $c matches\"; done && echo '=== REQUIRED ===' && grep -c 'dir=\"rtl\"' \"$FILE\" && grep -c 'max-width:780px' \"$FILE\" && grep -c 'text-align:right' \"$FILE\" && grep -c 'clamp(' \"$FILE\" && grep -c '#c8a97e' \"$FILE\" && echo '=== STRUCTURE ===' && head -1 \"$FILE\" | grep -q '<article' && echo 'STARTS WITH ARTICLE' && tail -1 \"$FILE\" | grep -q '</article>' && echo 'ENDS WITH ARTICLE' && wc -l < \"$FILE\""</automated>
  </verify>
  <acceptance_criteria>
    - All forbidden pattern counts are 0
    - dir="rtl" count >= 1
    - max-width:780px count >= 1
    - text-align:right count >= 5 (one per text element)
    - clamp( count >= 2 (intro paragraphs + summary title)
    - #c8a97e count >= 1
    - File starts with article tag, ends with article close tag
    - Line count between 30 and 80
  </acceptance_criteria>
  <done>
    - Zero forbidden patterns in the file
    - All required patterns present with correct counts
    - File structure validated as HTML fragment
    - Foundation is ready for Plan 02 to add heading hierarchy and section stubs
  </done>
</task>

</tasks>

<verification>
After both tasks complete, the file at `claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html` must:
1. Be a valid HTML fragment wrapped entirely in `<article dir="rtl" lang="he">...</article>`
2. Contain zero style blocks, zero class attributes, zero display properties
3. Have an "In This Article" summary card with decided colors
4. Use clamp() for responsive font sizing
5. Have explicit text-align:right on all text elements
</verification>

<success_criteria>
- TMPL-01: Article wrapped in article tags, nothing outside = VERIFIED by grep
- TMPL-02: dir="rtl" lang="he" present = VERIFIED by grep
- TMPL-03: Zero style blocks, all inline = VERIFIED by grep count 0
- TMPL-07: No external CSS/JS/CDN = VERIFIED by grep count 0
- TMPL-08: No HTML comments/markdown/code fences = VERIFIED by grep count 0
</success_criteria>

<output>
After completion, create `.planning/phases/03-template-foundation-article-structure/03-01-SUMMARY.md`
</output>
