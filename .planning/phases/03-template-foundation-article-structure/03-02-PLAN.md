---
phase: 03-template-foundation-article-structure
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html
autonomous: true
requirements:
  - TMPL-04
  - TMPL-05
  - TMPL-06

must_haves:
  truths:
    - "Responsive layout works using clamp()/percentages with no media queries"
    - "Semantic H2/H3 heading hierarchy is in place with premium editorial whitespace"
    - "Stub sections exist for TOC, Products, FAQ, and Author so Phases 4-8 can inject content"
    - "Section spacing is 48px between major sections, 24px within sections"
    - "Heading scale uses clamp() for responsive sizing without media queries"
  artifacts:
    - path: "claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html"
      provides: "Complete foundation with heading hierarchy, responsive layout, stub sections"
      min_lines: 80
      contains: "<h2"
  key_links:
    - from: "H2 section headings"
      to: "section containers"
      via: "id attributes for anchor targets"
      pattern: "id=\"[a-z-]+\""
    - from: "clamp() font sizes"
      to: "all text elements"
      via: "inline style font-size:clamp("
      pattern: "font-size:clamp\\("
---

<objective>
Add semantic H2/H3 heading hierarchy, responsive layout using clamp()/percentages, premium editorial whitespace, and placeholder stub sections for Phases 4-8 content.

Purpose: Completes the foundation so every subsequent phase has a marked insertion point. The heading hierarchy establishes the SEO structure and TOC anchor targets. The responsive typography ensures the article looks premium across all viewports without media queries.

Output: Updated `claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html` with full section structure.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-template-foundation-article-structure/03-CONTEXT.md
@.planning/phases/03-template-foundation-article-structure/03-01-SUMMARY.md
@.planning/phases/01-baseline-audit-wordpress-risk-assessment/audit/05-wordpress-rendering-risk-summary.md

<interfaces>
<!-- From Plan 03-01 output: the HTML file has: -->
<!-- <article id="hs-top" dir="rtl" lang="he" style="max-width:780px;..."> -->
<!--   2 intro paragraphs -->
<!--   "In This Article" summary card (<section> with bg #f9f6f1) -->
<!-- </article> -->

<!-- Design decisions from CONTEXT.md: -->
<!-- H2: font-size:clamp(22px,3vw,28px); font-weight:700; color:#1a1a1a; text-align:right -->
<!-- H3: font-size:clamp(18px,2.5vw,22px); font-weight:700; color:#1a1a1a; text-align:right -->
<!-- Section spacing: 48px margin-top between major sections -->
<!-- Within-section spacing: 24px -->
<!-- Section divider: border-top:1px solid #e8e0d4 (warm neutral) -->
<!-- Accent color: #c8a97e -->
<!-- Body text: color:#2d2d2d, font-size:clamp(16px,2.5vw,19px) -->

<!-- WordPress-safe layout approach (from Phase 1 audit Option B): -->
<!-- Use text-align:center on parent for centering -->
<!-- Use percentage widths + calc() for column layouts -->
<!-- Use vertical-align:middle for inline alignment -->
<!-- NO display property, NO media queries, NO class attributes -->

<!-- Stub sections needed for downstream phases: -->
<!-- Phase 4: TOC section (details/summary accordion) -->
<!-- Phase 5: Products section (product cards grid) -->
<!-- Phase 6: FAQ section (individual accordions) -->
<!-- Phase 7: Author section (bio + social links) + Floating buttons -->
<!-- Phase 8: JSON-LD schema (script blocks at end) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add semantic heading hierarchy and stub sections with editorial spacing</name>
  <files>claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html</files>
  <read_first>
    - claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html
    - .planning/phases/03-template-foundation-article-structure/03-CONTEXT.md
  </read_first>
  <action>
Read the current file created by Plan 03-01. Insert section stubs AFTER the "In This Article" summary card and BEFORE the closing `</article>` tag.

Add stub sections in this exact order (this is the article's document flow):

1. **TOC stub** (Phase 4 will replace):
```html
<!-- PHASE-4-TOC-START -->
<details style="margin:48px 0 0;border:1px solid #e8e0d4;border-radius:12px;background:#f9f6f1;overflow:hidden">
  <summary style="cursor:pointer;list-style:none;padding:15px 18px;font-size:clamp(16px,2.5vw,19px);font-weight:700;color:#1a1a1a;text-align:right">תוכן עניינים</summary>
  <nav style="padding:0 18px 18px;text-align:right">
    <p style="margin:0;color:#2d2d2d;font-size:clamp(14px,2vw,16px);text-align:right">Phase 4 will populate TOC links here</p>
  </nav>
</details>
<!-- PHASE-4-TOC-END -->
```
WAIT — no HTML comments allowed (TMPL-08). Use visible placeholder text instead of HTML comments. Remove all `<!-- -->` markers. Instead, the stubs are self-documenting by their content.

Corrected approach — NO HTML comments anywhere. Just use the stub sections directly:

**TOC stub** (Phase 4 replaces this details element):
```html
<details style="margin:48px 0 0;border:1px solid #e8e0d4;border-radius:12px;background:#f9f6f1;overflow:hidden">
  <summary style="cursor:pointer;list-style:none;padding:15px 18px;font-size:clamp(16px,2.5vw,19px);font-weight:700;color:#1a1a1a;text-align:right">תוכן עניינים</summary>
  <nav style="padding:0 18px 18px;text-align:right">
    <p style="margin:0;color:#2d2d2d;font-size:clamp(14px,2vw,16px);text-align:right">TOC links placeholder</p>
  </nav>
</details>
```

2. **Body content sections** with H2 headings (these are the main article sections). Use 6 placeholder H2 sections that match typical article structure. Each section gets:
   - A `<section>` wrapper with `style="margin:48px 0 0;padding:48px 0 0;border-top:1px solid #e8e0d4"` (section divider per decision)
   - An `<h2>` with `id` attribute for anchor linking, styled: `style="margin:0 0 24px;font-size:clamp(22px,3vw,28px);font-weight:700;color:#1a1a1a;line-height:1.4;text-align:right"`
   - A placeholder `<p>` styled: `style="margin:0 0 1.4rem;font-size:clamp(16px,2.5vw,19px);line-height:1.85;color:#2d2d2d;text-align:right"`
   - The FIRST section should NOT have the border-top divider (it follows TOC directly)

Section IDs and Hebrew placeholder headings (N8N will replace these):
- id="section-1" — כותרת ראשית ראשונה
- id="section-2" — כותרת ראשית שנייה
- id="section-3" — כותרת ראשית שלישית
- id="section-4" — כותרת ראשית רביעית
- id="section-5" — כותרת ראשית חמישית
- id="section-6" — כותרת ראשית שישית

Each section's placeholder paragraph: תוכן המקטע יוזן על ידי מערכת N8N בעת יצירת המאמר.

3. **Products stub section** (Phase 5 replaces inner content):
```html
<section id="products" style="margin:48px 0 0;padding:48px 0 0;border-top:1px solid #e8e0d4">
  <h2 style="margin:0 0 24px;font-size:clamp(22px,3vw,28px);font-weight:700;color:#1a1a1a;line-height:1.4;text-align:right">מוצרים מומלצים</h2>
  <p style="margin:0;font-size:clamp(16px,2.5vw,19px);line-height:1.85;color:#2d2d2d;text-align:right">Phase 5 will insert product cards here</p>
</section>
```

4. **FAQ stub section** (Phase 6 replaces inner content):
```html
<section id="faq" style="margin:48px 0 0;padding:48px 0 0;border-top:1px solid #e8e0d4">
  <h2 style="margin:0 0 24px;font-size:clamp(22px,3vw,28px);font-weight:700;color:#1a1a1a;line-height:1.4;text-align:right">שאלות נפוצות</h2>
  <p style="margin:0;font-size:clamp(16px,2.5vw,19px);line-height:1.85;color:#2d2d2d;text-align:right">Phase 6 will insert FAQ accordions here</p>
</section>
```

5. **Author stub section** (Phase 7 replaces — MUST be last content section per AUTH-01):
```html
<section id="author" style="margin:48px 0 0;padding:48px 0 0;border-top:1px solid #e8e0d4">
  <h2 style="margin:0 0 24px;font-size:clamp(22px,3vw,28px);font-weight:700;color:#1a1a1a;line-height:1.4;text-align:right">אודות הכותב</h2>
  <p style="margin:0;font-size:clamp(16px,2.5vw,19px);line-height:1.85;color:#2d2d2d;text-align:right">Phase 7 will insert author bio and social links here</p>
</section>
```

Document order MUST be: Intro > Summary Card > TOC > Body Sections (1-6) > Products > FAQ > Author > (closing article tag).

CRITICAL RULES:
- Zero `display:` property anywhere
- Zero `class=` attributes anywhere
- Zero `<style` blocks
- Zero `<!--` HTML comments
- Zero `@media` queries
- Every text element has explicit `text-align:right`
- All font sizes use `clamp()` for responsive behavior (TMPL-04)
- 48px spacing between major sections (TMPL-06)
- Section dividers use `border-top:1px solid #e8e0d4` (TMPL-06)
  </action>
  <verify>
    <automated>bash -c "FILE='claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html' && echo '=== HEADINGS ===' && grep -c '<h2' \"$FILE\" && echo '=== SECTIONS ===' && grep -c 'id=\"section-' \"$FILE\" && echo '=== STUBS ===' && grep -c 'id=\"products\"' \"$FILE\" && grep -c 'id=\"faq\"' \"$FILE\" && grep -c 'id=\"author\"' \"$FILE\" && echo '=== SPACING ===' && grep -c 'margin:48px' \"$FILE\" && grep -c 'border-top:1px solid #e8e0d4' \"$FILE\" && echo '=== RESPONSIVE ===' && grep -c 'clamp(' \"$FILE\" && echo '=== FORBIDDEN ===' && grep -ci 'display:' \"$FILE\" || echo '0' && grep -c 'class=' \"$FILE\" || echo '0' && grep -c '<!--' \"$FILE\" || echo '0' && grep -c '@media' \"$FILE\" || echo '0' && echo '=== LINE COUNT ===' && wc -l < \"$FILE\""</automated>
  </verify>
  <acceptance_criteria>
    - grep '<h2' count >= 9 (6 body sections + products + faq + author)
    - grep 'id="section-' count = 6
    - grep 'id="products"' count = 1
    - grep 'id="faq"' count = 1
    - grep 'id="author"' count = 1
    - grep 'margin:48px' count >= 8 (section spacing)
    - grep 'border-top:1px solid #e8e0d4' count >= 7 (section dividers, first body section excluded)
    - grep 'clamp(' count >= 20 (every heading + paragraph uses clamp font-size)
    - grep -i 'display:' count = 0
    - grep 'class=' count = 0
    - grep '<!--' count = 0
    - grep '@media' count = 0
    - Line count between 80 and 150
  </acceptance_criteria>
  <done>
    - 9+ H2 headings present with correct clamp() sizing
    - 6 body content sections with unique IDs for anchor linking
    - Products, FAQ, Author stub sections present in correct order
    - Author section is last content before closing article tag
    - 48px section spacing and border-top dividers throughout
    - All font sizes use clamp() for responsive behavior
    - Zero forbidden patterns (display, class, style blocks, comments, media queries)
  </done>
</task>

<task type="auto">
  <name>Task 2: Final comprehensive validation of complete foundation template</name>
  <files>claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html</files>
  <read_first>
    - claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html
  </read_first>
  <action>
Run final validation covering ALL Phase 3 requirements (TMPL-01 through TMPL-08):

**Structural validation:**
1. File starts with `<article` (first non-whitespace content)
2. File ends with `</article>` (last non-whitespace content)
3. No content exists outside the article wrapper
4. `dir="rtl"` and `lang="he"` on the article tag

**CSS architecture validation:**
5. Zero `<style` blocks (TMPL-03)
6. Zero `class=` attributes (TMPL-03 / WP safety)
7. Zero `display:` property occurrences (WP safety)
8. Every visible element has inline `style=` attribute

**Responsive validation (TMPL-04):**
9. Zero `@media` queries
10. All `<h2>` elements use `font-size:clamp(22px,3vw,28px)`
11. All `<p>` elements use `font-size:clamp(16px,2.5vw,19px)`
12. At least one `clamp()` usage exists

**Semantic hierarchy validation (TMPL-05):**
13. Count of `<h2` >= 9 (body sections + product + faq + author)
14. Zero `<h1` tags (H1 is provided by WordPress post title)
15. All H2s have `id=` attributes for anchor linking

**Clean output validation (TMPL-07, TMPL-08):**
16. Zero `<link` tags
17. Zero `<script src=` tags
18. Zero `<!--` HTML comments
19. Zero markdown syntax (```, ##)
20. Zero `http://` (only https or no protocol)

**Editorial typography validation (TMPL-06):**
21. At least 7 occurrences of `border-top:1px solid #e8e0d4` (section dividers)
22. At least 8 occurrences of `margin:48px` (section spacing)
23. `color:#1a1a1a` on all headings
24. `color:#2d2d2d` on all body text

**Document order validation:**
25. The `id="author"` section appears AFTER `id="faq"` in file
26. The `id="faq"` section appears AFTER `id="products"` in file
27. The TOC details element appears BEFORE `id="section-1"` in file

If ANY check fails, fix the issue in the HTML file and re-run validation. Do not skip fixes.
  </action>
  <verify>
    <automated>bash -c "FILE='claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html' && echo '--- TMPL-01 ---' && head -1 \"$FILE\" | grep -q '<article' && echo 'PASS: starts with article' || echo 'FAIL' && tail -1 \"$FILE\" | grep -q '</article>' && echo 'PASS: ends with article' || echo 'FAIL' && echo '--- TMPL-02 ---' && grep -q 'dir=\"rtl\"' \"$FILE\" && echo 'PASS: RTL' || echo 'FAIL' && grep -q 'lang=\"he\"' \"$FILE\" && echo 'PASS: lang=he' || echo 'FAIL' && echo '--- TMPL-03 ---' && ! grep -q '<style' \"$FILE\" && echo 'PASS: no style blocks' || echo 'FAIL' && ! grep -q 'class=' \"$FILE\" && echo 'PASS: no classes' || echo 'FAIL' && echo '--- TMPL-04 ---' && ! grep -q '@media' \"$FILE\" && echo 'PASS: no media queries' || echo 'FAIL' && grep -q 'clamp(' \"$FILE\" && echo 'PASS: clamp used' || echo 'FAIL' && echo '--- TMPL-05 ---' && echo \"H2 count: $(grep -c '<h2' \"$FILE\")\" && ! grep -q '<h1' \"$FILE\" && echo 'PASS: no H1' || echo 'FAIL' && echo '--- TMPL-06 ---' && echo \"Section dividers: $(grep -c 'border-top:1px solid #e8e0d4' \"$FILE\")\" && echo \"Section spacing: $(grep -c 'margin:48px' \"$FILE\")\" && echo '--- TMPL-07 ---' && ! grep -q '<link' \"$FILE\" && echo 'PASS: no link tags' || echo 'FAIL' && ! grep -q '<script src' \"$FILE\" && echo 'PASS: no script src' || echo 'FAIL' && echo '--- TMPL-08 ---' && ! grep -q '<!--' \"$FILE\" && echo 'PASS: no comments' || echo 'FAIL' && echo '--- WP SAFETY ---' && ! grep -qi 'display:' \"$FILE\" && echo 'PASS: no display property' || echo 'FAIL' && echo '--- TOTAL LINES ---' && wc -l < \"$FILE\" && echo '--- ALL DONE ---'"</automated>
  </verify>
  <acceptance_criteria>
    - TMPL-01: starts with article, ends with article = PASS
    - TMPL-02: dir="rtl" and lang="he" present = PASS
    - TMPL-03: no style blocks, no classes = PASS
    - TMPL-04: no media queries, clamp() used = PASS
    - TMPL-05: H2 count >= 9, no H1 = PASS
    - TMPL-06: section dividers >= 7, section spacing >= 8 = PASS
    - TMPL-07: no link tags, no script src = PASS
    - TMPL-08: no HTML comments = PASS
    - WP safety: no display property = PASS
    - Total lines between 80 and 150
  </acceptance_criteria>
  <done>
    - All 8 TMPL requirements verified with automated grep checks
    - WordPress safety validated (no display property)
    - Foundation template is complete and ready for Phase 4+ section builds
    - Document order confirmed: Intro > Summary > TOC > Body Sections > Products > FAQ > Author
  </done>
</task>

</tasks>

<verification>
After both tasks complete, the file at `claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html` must:
1. Pass ALL 8 TMPL requirement checks
2. Have 9+ H2 headings with clamp() responsive sizing
3. Have stub sections for TOC, Products, FAQ, Author in correct order
4. Have 48px section spacing with border-top dividers
5. Contain zero WordPress-unsafe patterns (display, style blocks, classes, media queries)
6. Be between 80-150 lines total
</verification>

<success_criteria>
- TMPL-04: Responsive layout via clamp()/percentages, zero media queries = VERIFIED
- TMPL-05: Semantic H2 hierarchy, all with id attributes = VERIFIED
- TMPL-06: Premium editorial whitespace (48px spacing, 24px heading margin, dividers) = VERIFIED
- All body section headings have anchor-ready id attributes for Phase 4 TOC linking
- Stub sections provide clear insertion points for Phases 4-8
</success_criteria>

<output>
After completion, create `.planning/phases/03-template-foundation-article-structure/03-02-SUMMARY.md`
</output>
