---
phase: 01-baseline-audit-wordpress-risk-assessment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-baseline-audit-wordpress-risk-assessment/audit/02-prompt-contradiction-report.md
  - .planning/phases/01-baseline-audit-wordpress-risk-assessment/audit/03-workflow-logic-bug-report.md
autonomous: true
requirements:
  - DEL-07

must_haves:
  truths:
    - "Every internal contradiction in the N8N prompt TXT is documented with exact line numbers on both sides of the contradiction"
    - "Every logic bug in the N8N workflow JSON is documented with node name, code excerpt, and failure scenario"
    - "The style block contradiction (prompt says include it AND says forbidden) is explicitly called out"
    - "The JSON-LD contradiction (prompt says no JSON-LD but template has it) is documented"
    - "The Clean HTML node self-contradiction (preserves style block then rejects it) is documented"
    - "The WordPress Publish content escaping bug is documented"
    - "All oritmartin-specific hardcoded data in prompt and workflow is flagged"
  artifacts:
    - path: ".planning/phases/01-baseline-audit-wordpress-risk-assessment/audit/02-prompt-contradiction-report.md"
      provides: "Complete N8N prompt TXT contradiction and issue report"
      contains: "Contradiction"
    - path: ".planning/phases/01-baseline-audit-wordpress-risk-assessment/audit/03-workflow-logic-bug-report.md"
      provides: "Complete N8N workflow JSON logic bug report"
      contains: "BUG"
  key_links:
    - from: "02-prompt-contradiction-report.md"
      to: "wp-n8n-html-design-improver/Improved_N8N_Prompt-claude-code-2026-03-25.txt"
      via: "line-number references"
      pattern: "Line \\d+"
    - from: "03-workflow-logic-bug-report.md"
      to: "wp-n8n-html-design-improver/Improved_N8N_Workflow-claude-code-2026-03-25.json"
      via: "node name and line references"
      pattern: "Node:|Line \\d+"
---

<objective>
Audit the existing N8N prompt TXT (322 lines) and N8N workflow JSON (683 lines) for internal contradictions, logic bugs, cross-file drift, and oritmartin-specific hardcoded data. Produce two separate reports.

Purpose: The prompt and workflow contain critical contradictions (style block mandated AND forbidden) and logic bugs (Clean HTML node always fails) that must be documented before Phase 9 rebuilds them. Without this audit, Phase 9 will unknowingly replicate the same bugs.

Output: Two markdown files -- a prompt contradiction report and a workflow logic bug report -- in the audit/ subdirectory.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-audit-wordpress-risk-assessment/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: N8N Prompt Contradiction Report</name>
  <files>
    .planning/phases/01-baseline-audit-wordpress-risk-assessment/audit/02-prompt-contradiction-report.md
  </files>
  <read_first>
    wp-n8n-html-design-improver/Improved_N8N_Prompt-claude-code-2026-03-25.txt
    wp-n8n-html-design-improver/Improved_HTML_Template-claude-code-2026-03-25.html
    .planning/phases/01-baseline-audit-wordpress-risk-assessment/01-RESEARCH.md
  </read_first>
  <action>
    Read the full 322-line N8N prompt TXT. Cross-reference with the HTML template for drift.

    Create `02-prompt-contradiction-report.md` with these sections:

    **1. Executive Summary** -- total contradictions by severity, overall prompt health assessment

    **2. CRITICAL Contradictions** -- table: ID, Line A, Says, Line B, Says (contradicts), Impact, Resolution Strategy

    Must document at minimum:
    - CRIT-1: Style Block Contradiction
      - Line 18: "Inline CSS on each element PLUS one `<style>` block inside the article"
      - Line ~292 area: "Style blocks" listed in FORBIDDEN PATTERNS
      - Line ~320 area: QA CHECKLIST says "No style block exists"
      - Lines 29-44: "MANDATORY CSS BLOCK" section with exact `<style>` content
      - Line ~282 area: "No html, head, body, or style wrapper"
      Find exact line numbers with grep -- do not use approximate numbers.

    - CRIT-2: JSON-LD Contradiction
      - Prompt line ~26: "No JSON-LD block at the end"
      - Template lines 312-319: Three JSON-LD script blocks present after author section
      Find exact lines with grep.

    **3. HIGH Issues** -- same table format
    Must document:
    - HIGH-1: Prompt instructs LLM to produce patterns that WordPress strips (display:flex references in prompt if any)
    - HIGH-2: All oritmartin.com hardcoded data in prompt (lines 59-68 verified site facts, lines 107-108 gallery refs, lines 208-213/240-241/250-251/257-261 brand instructions, lines 269-277 brand colors)
    - HIGH-3: Prompt-to-template drift -- instructions in prompt that don't match what the template actually does
    Find exact lines with grep for "oritmartin", "orit", "display", "style", "flex", "grid".

    **4. MEDIUM Issues**
    - MED-1: QA checklist items that contradict body instructions
    - MED-2: Ambiguous or conflicting CSS instructions
    - MED-3: Any N8N expression syntax issues (grep for `{{ ` patterns)

    **5. Cross-File Drift Matrix** -- table comparing what the prompt instructs vs what the template actually contains for each major feature:
    | Feature | Prompt Says | Template Has | Match? | Issue |
    Cover: style block, JSON-LD, accordion structure, product cards, floating buttons, author section, social links, RTL handling, heading hierarchy

    **6. N8N Expression Audit** -- list every `{{ }}` expression in the prompt, verify syntax correctness:
    - `{{ JSON.stringify($json["products"], null, 2) }}`
    - `{{ $("Writing Blog").first().json.output }}`
    - Any others found via grep

    **7. Recommendations for Phase 9** -- prioritized list of what must change when the prompt is rebuilt
  </action>
  <verify>
    <automated>grep -c "CRIT-\|HIGH-\|MED-\|Contradiction" ".planning/phases/01-baseline-audit-wordpress-risk-assessment/audit/02-prompt-contradiction-report.md"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/01-baseline-audit-wordpress-risk-assessment/audit/02-prompt-contradiction-report.md`
    - File contains "Executive Summary" section
    - File contains "CRITICAL Contradictions" section with at least CRIT-1 and CRIT-2
    - File contains "HIGH Issues" section with at least HIGH-1 through HIGH-3
    - File contains "Cross-File Drift Matrix" section with table rows for at least 8 features
    - File contains "N8N Expression Audit" section
    - File contains "Recommendations for Phase 9" section
    - grep -c "Line" on this file returns > 10 (line number references throughout)
    - grep -c "oritmartin" on this file returns > 0 (brand reference issues documented)
    - Every contradiction entry has both "Line A" and "Line B" (both sides documented)
  </acceptance_criteria>
  <done>Complete prompt contradiction report with every internal inconsistency documented, cross-file drift mapped, and Phase 9 recommendations listed. Contributes to DEL-07 before-state audit.</done>
</task>

<task type="auto">
  <name>Task 2: N8N Workflow Logic Bug Report</name>
  <files>
    .planning/phases/01-baseline-audit-wordpress-risk-assessment/audit/03-workflow-logic-bug-report.md
  </files>
  <read_first>
    wp-n8n-html-design-improver/Improved_N8N_Workflow-claude-code-2026-03-25.json
    wp-n8n-html-design-improver/Improved_N8N_Prompt-claude-code-2026-03-25.txt
    .planning/phases/01-baseline-audit-wordpress-risk-assessment/01-RESEARCH.md
  </read_first>
  <action>
    Read the full 683-line N8N workflow JSON. Analyze every node for logic bugs, contradictions, and alignment issues.

    Write `03-workflow-logic-bug-report.md` with these sections:

    **1. Executive Summary** -- total bugs by severity, workflow health assessment, node count

    **2. Workflow Node Map** -- list every node in the workflow with: name, type, line number in JSON, brief purpose. This provides the structural overview for Phase 9.

    **3. CRITICAL Bugs** -- table: ID, Node Name, Line(s), Bug Description, Impact, Fix Strategy

    Must document at minimum:
    - BUG-1: Clean HTML Node Self-Contradiction (line ~302 area)
      - Step 1: `html.replace(/<style>(?!\s*#om-top)[\s\S]*?<\/style>/gi, '')` tries to PRESERVE #om-top style block
      - Step 2: `if (/<style\b/i.test(html)) throw new Error(...)` rejects ANY style block
      - Result: If LLM obeys prompt's MANDATORY CSS BLOCK instruction, workflow ALWAYS fails
      - Fix: Strip ALL style blocks unconditionally (align with WordPress reality)

    - BUG-2: Build Final HTML Prompt Contradiction (line ~261 area)
      - Embedded prompt says "MUST include one `<style>` block" AND "Inline CSS only" in consecutive instructions
      - Fix: Remove style block instruction, keep inline CSS only

    **4. HIGH Bugs**
    - BUG-3: LLM API Uses Maximo Credentials
      - Nodes: "Write Blog Draft (LLM)" and "Render Final HTML (LLM)"
      - Uses `={{$credentials.maximoApiUrl}}/api/v1/generate` with `maximo-api-key`
      - Question: Are these correct for hipsterstyle deployment?

    - BUG-4: WordPress Publish Content Escaping (line ~334 area)
      - `"content": "{{ $json.content }}"` -- raw expression interpolation into JSON
      - HTML with double quotes will break the JSON structure
      - Fix: Use Code node to JSON.stringify the content before injection

    **5. MEDIUM Bugs**
    - BUG-5: Error Workflow Not Connected
      - Error Handler (Slack) node exists but `settings.errorWorkflow` is empty string
      - Error handler not in connections map
      - Fix: Connect error handler or remove dead node

    - BUG-6: Any additional N8N expression issues found
    - BUG-7: Any node connection gaps or orphaned nodes

    **6. Workflow-to-Prompt Alignment** -- for each node that contains embedded prompt text or references prompt output, verify alignment:
    | Node | References | Matches Prompt? | Issue |

    **7. Workflow-to-Template Alignment** -- for each node that processes or validates HTML, verify it matches the template structure:
    | Node | Validates/Processes | Matches Template? | Issue |

    **8. oritmartin References in Workflow** -- every hardcoded oritmartin reference with line number, node name, and required change

    **9. Recommendations for Phase 9** -- prioritized list of what must change when the workflow is rebuilt

    Use exact line numbers from grep/search output. Include actual code excerpts (10-15 lines max each) for critical bugs.
  </action>
  <verify>
    <automated>grep -c "BUG-\|CRITICAL\|HIGH\|MEDIUM" ".planning/phases/01-baseline-audit-wordpress-risk-assessment/audit/03-workflow-logic-bug-report.md"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/01-baseline-audit-wordpress-risk-assessment/audit/03-workflow-logic-bug-report.md`
    - File contains "Executive Summary" section
    - File contains "Workflow Node Map" section listing all nodes
    - File contains "CRITICAL Bugs" section with at least BUG-1 and BUG-2
    - File contains "HIGH Bugs" section with at least BUG-3 and BUG-4
    - File contains "MEDIUM Bugs" section with at least BUG-5
    - File contains "Workflow-to-Prompt Alignment" section
    - File contains "Workflow-to-Template Alignment" section
    - File contains "Recommendations for Phase 9" section
    - grep -c "BUG-" on this file returns >= 5 (at least 5 bugs documented)
    - grep -c "Node" on this file returns > 5 (node references throughout)
    - At least one code excerpt block (```) present showing the actual buggy code
  </acceptance_criteria>
  <done>Complete workflow logic bug report with every node mapped, all bugs severity-classified, and Phase 9 recommendations listed. Contributes to DEL-07 before-state audit.</done>
</task>

</tasks>

<verification>
1. `ls .planning/phases/01-baseline-audit-wordpress-risk-assessment/audit/02-prompt-contradiction-report.md` -- file exists
2. `ls .planning/phases/01-baseline-audit-wordpress-risk-assessment/audit/03-workflow-logic-bug-report.md` -- file exists
3. `grep -c "CRIT-" .planning/phases/01-baseline-audit-wordpress-risk-assessment/audit/02-prompt-contradiction-report.md` -- returns >= 2
4. `grep -c "BUG-" .planning/phases/01-baseline-audit-wordpress-risk-assessment/audit/03-workflow-logic-bug-report.md` -- returns >= 5
</verification>

<success_criteria>
- Every internal contradiction in the prompt is documented with exact line numbers on both sides
- Every logic bug in the workflow is documented with node name, line numbers, and code excerpt
- Cross-file alignment (prompt vs template, workflow vs prompt, workflow vs template) is verified
- All oritmartin-specific references in both files are catalogued
- Phase 9 recommendations are actionable and prioritized
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-audit-wordpress-risk-assessment/01-02-SUMMARY.md`
</output>
