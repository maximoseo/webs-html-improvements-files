---
phase: 04-navigation-table-of-contents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - NAV-05
  - NAV-06
  - NAV-07
  - HOVER-01

must_haves:
  truths:
    - "TOC accordion exists near article top, after intro/summary card, closed by default"
    - "Opening the accordion reveals 9 TOC links in a single column with no numbering"
    - "Each TOC link scrolls to the correct H2 section when clicked"
    - "Hovering a TOC link shows underline and color shift to #c8a97e"
    - "Plus/minus indicator animates on accordion open/close"
    - "Title reads exactly 'תוכן עניינים'"
  artifacts:
    - path: "claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html"
      provides: "Full TOC section with anchor links, hover handlers, and indicator animation"
      contains: "תוכן עניינים"
  key_links:
    - from: "TOC anchor links"
      to: "H2 section IDs (section-1 through section-6, products, faq, author)"
      via: "href=#id anchors"
      pattern: "href=\"#(section-[1-6]|products|faq|author)\""
---

<objective>
Replace the TOC placeholder in the HTML template with a fully working Table of Contents: 9 anchor links pointing to every H2 section, hover states via inline onmouseover/onmouseout, and a plus/minus indicator that toggles on accordion open/close.

Purpose: Users can navigate the article via a polished, WordPress-safe TOC
Output: Updated HTML template with complete TOC section
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-navigation-table-of-contents/04-CONTEXT.md

<interfaces>
<!-- Current template TOC stub (lines 46-51) to be replaced: -->

```html
<details style="margin:48px 0 0;border:1px solid #e8e0d4;border-radius:12px;background:#f9f6f1;overflow:hidden">
  <summary style="cursor:pointer;list-style:none;padding:15px 18px;font-size:clamp(16px,2.5vw,19px);font-weight:700;color:#1a1a1a;text-align:right">תוכן עניינים</summary>
  <nav style="padding:0 18px 18px;text-align:right">
    <p style="margin:0;color:#2d2d2d;font-size:clamp(14px,2vw,16px);text-align:right">TOC links placeholder</p>
  </nav>
</details>
```

<!-- H2 section IDs in the template (in document order): -->
<!-- section-1: כותרת ראשית ראשונה -->
<!-- section-2: כותרת ראשית שנייה -->
<!-- section-3: כותרת ראשית שלישית -->
<!-- section-4: כותרת ראשית רביעית -->
<!-- section-5: כותרת ראשית חמישית -->
<!-- section-6: כותרת ראשית שישית -->
<!-- products: מוצרים מומלצים -->
<!-- faq: שאלות נפוצות -->
<!-- author: אודות הכותב -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace TOC stub with full anchor links, hover states, and plus/minus indicator</name>
  <files>claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html</files>
  <action>
Replace lines 46-51 (the entire details block) with a new details block. Keep the same outer details/summary structure and styling. Requirements per locked decisions:

**A) Summary element with plus/minus indicator (NAV-02, NAV-05, NAV-07):**
- Keep summary text as exactly "תוכן עניינים"
- Append a span containing "+" as the indicator character, positioned on the left side (since RTL, use float:left or margin-inline-start:auto approach via inline style — but NO display property allowed, so use float:left)
- Add inline onmouseover/onmouseout to the summary for subtle hover feedback
- Add an ontoggle handler on the details element that swaps "+" to "−" (minus sign U+2212) when open, and back to "+" when closed. The handler targets the indicator span by finding it inside the details element. Example: `ontoggle="this.querySelector('[data-toc-icon]').textContent=this.open?'\u2212':'+'"` — BUT data attributes might be stripped by WP, so instead use a positional approach: `ontoggle="this.firstElementChild.lastElementChild.textContent=this.open?'\u2212':'+'"` (summary is firstElementChild, indicator span is last child of summary)

**B) Nav element with 9 anchor links (NAV-01, NAV-03, NAV-04):**
- Inside the nav, create 9 `<a>` elements, one per H2 section, in document order
- Each link: `href="#section-1"` (or #products, #faq, #author)
- Each link text = the H2 text from the template (Hebrew titles as listed in interfaces block above)
- Each link on its own line — use `<div>` wrapper per link for single-column layout (div has no display issue, is block by default)
- NO numbering, NO list elements (no ul/ol/li)
- Style each link div: `style="padding:8px 0;border-bottom:1px solid #eee;text-align:right"`
- Last link div: no border-bottom
- Style each anchor: `style="color:#4a4a4a;text-decoration:none;font-size:clamp(14px,2vw,16px);line-height:1.6"`

**C) Hover states on each TOC link (NAV-06, HOVER-01):**
- Each anchor gets inline handlers:
  - `onmouseover="this.style.color='#c8a97e';this.style.textDecoration='underline'"`
  - `onmouseout="this.style.color='#4a4a4a';this.style.textDecoration='none'"`

**D) WordPress safety rules:**
- Zero style blocks, zero class attributes, zero display property in any inline style
- All interactivity via inline event handlers only (onmouseover, onmouseout, ontoggle)
- No external JS, no CDN, no HTML comments in output
- details/summary is WordPress-safe per Phase 1 audit (WP-03)

**E) Structural placement:**
- The details block stays in its current position (after the "במאמר זה" summary card, before section-1)
- Keep the same margin-top (48px) to maintain spacing from the summary card above
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html','utf8');const checks=[['details element exists',/<details[^>]*ontoggle/.test(h)],['summary has indicator span',/תוכן עניינים.*<span/.test(h)],['9 anchor links',((h.match(/href=\"#(section-[1-6]|products|faq|author)\"/g)||[]).length===9)],['hover handlers on links',((h.match(/onmouseover=\"this\.style\.color/g)||[]).length>=9)],['no style blocks',!/<style[\s>]/.test(h)],['no class attributes',!/\sclass=/.test(h)],['no display property',!/display\s*:/.test(h)],['closed by default (no open attr)',!/<details[^>]*\sopen[\s>]/.test(h)]];let pass=true;checks.forEach(([name,ok])=>{console.log((ok?'PASS':'FAIL')+': '+name);if(!ok)pass=false;});process.exit(pass?0:1);"</automated>
  </verify>
  <done>
- TOC details/summary block is closed by default with title "תוכן עניינים"
- 9 anchor links (section-1 through section-6, products, faq, author) in single column, no numbering
- Every link has onmouseover/onmouseout for underline + #c8a97e color shift
- Plus/minus indicator toggles via ontoggle handler on details element
- Zero style blocks, zero classes, zero display property
- Template validates all 8 automated checks
  </done>
</task>

</tasks>

<verification>
Open the HTML file in a browser and verify:
1. TOC accordion is closed on page load
2. Clicking opens it, revealing 9 links in a single column
3. "+" changes to "−" on open, back to "+" on close
4. Hovering any link shows underline and gold (#c8a97e) color
5. Clicking any link scrolls to the correct section
6. All inline — no style blocks, no classes, no display property
</verification>

<success_criteria>
- All 8 requirement IDs (NAV-01 through NAV-07, HOVER-01) satisfied
- Automated verification script passes all checks
- Template remains a single valid HTML file with zero WordPress-unsafe patterns
</success_criteria>

<output>
After completion, create `.planning/phases/04-navigation-table-of-contents/04-01-SUMMARY.md`
</output>
