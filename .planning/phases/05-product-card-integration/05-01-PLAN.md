---
phase: 05-product-card-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html
autonomous: true
requirements:
  - PROD-03
  - PROD-04
  - PROD-05
  - PROD-06
  - PROD-07
  - PROD-09
  - PROD-10

must_haves:
  truths:
    - "6 product cards visible with image above and title/CTA below"
    - "All cards are uniform size and visually aligned in rows"
    - "No text overlaid on images, no prices shown"
    - "Images display undistorted from Supabase URLs"
    - "Each card links to real hipsterstyle.co.il product page"
  artifacts:
    - path: "claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html"
      provides: "Product cards section replacing stub"
      contains: "supabase.co/storage/v1/object/public/article-assets/hipsterstyle"
  key_links:
    - from: "product card img src"
      to: "Supabase public URLs"
      via: "hardcoded img src attributes"
      pattern: "zqcnyllsfwnssnuduvmj\\.supabase\\.co"
    - from: "product card CTA link"
      to: "hipsterstyle.co.il product pages"
      via: "a href attributes"
      pattern: "hipsterstyle\\.co\\.il/products/"
---

<objective>
Replace the products stub in the HTML template with 6 real product cards using data from hipsterstyle-discovery.json. Each card: image above, title + CTA below, inline-block layout, no flex/grid, no style blocks, no classes, Supabase image URLs, no prices.

Purpose: Delivers PROD-03 through PROD-10 (minus PROD-08 already done) -- real product integration into WordPress-safe template.
Output: Updated HTML template with complete product cards section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-product-card-integration/05-CONTEXT.md

<interfaces>
From hipsterstyle-discovery.json -- 6 products to render:

1. name: "חליפת סנדי" | url: hipsterstyle.co.il/products/חליפת-סנדי | image: supabase...product-sandy.jpg
2. name: "חליפת הנרי" | url: hipsterstyle.co.il/products/חליפת-הנרי | image: supabase...product-henry.jpg
3. name: "חליפת נינו" | url: hipsterstyle.co.il/products/חליפת-נינו | image: supabase...product-nino.jpg
4. name: "חליפת מילה" | url: hipsterstyle.co.il/products/חליפת-מילה | image: supabase...product-mila.jpg
5. name: "טישרט אלי" | url: hipsterstyle.co.il/products/טישרט-אלי | image: supabase...product-eli.jpg
6. name: "ג'ינס שיי" | url: hipsterstyle.co.il/products/ג׳ינס-שיי | image: supabase...product-shay.jpg

Current template stub to replace (lines 109-112):
```html
<section id="products" style="margin:48px 0 0;padding:48px 0 0;border-top:1px solid #e8e0d4">
  <h2 style="...">מוצרים מומלצים</h2>
  <p style="...">Phase 5 will insert product cards here</p>
</section>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build 6 product cards replacing products stub</name>
  <files>claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html</files>
  <action>
Read the current template. Read hipsterstyle-discovery.json for exact product data (names, URLs, image URLs, alt text).

Replace the stub paragraph inside `<section id="products">` (the line "Phase 5 will insert product cards here") with 6 product cards. Keep the existing section wrapper and H2 heading intact.

**Card container:** Add a wrapping div with `style="text-align:center;margin:24px 0 0"` to center the inline-block cards.

**Each card structure (per D-03, D-04, D-05, D-06, D-07, D-09, D-10):**
```
<div style="display:inline-block;vertical-align:top;width:30%;min-width:220px;margin:12px 1.5%;text-align:center">
  <a href="{product.url}" target="_blank" rel="noopener" style="text-decoration:none;color:inherit">
    <div style="background:#f9f6f1;border:1px solid #e8e0d4;border-radius:12px;overflow:hidden;padding-bottom:16px">
      <img src="{product.image}" alt="{product.image_alt}" style="width:100%;height:auto;object-fit:contain" />
      <p style="margin:12px 8px 8px;font-size:clamp(15px,2.2vw,18px);font-weight:700;color:#1a1a1a;line-height:1.4;text-align:center">{product.name}</p>
      <span style="display:inline-block;margin:4px auto 0;padding:8px 20px;background:#c8a97e;color:#ffffff;border-radius:8px;font-size:clamp(13px,2vw,15px);font-weight:600;text-align:center"
        onmouseover="this.style.background='#b89968'" onmouseout="this.style.background='#c8a97e'">לצפייה במוצר</span>
    </div>
  </a>
</div>
```

Key rules:
- display:inline-block with vertical-align:top for card alignment (per locked decision -- NO flex/grid)
- width:30% with min-width:220px naturally wraps to 2-col tablet and 1-col mobile
- img tag with width:100%, height:auto, object-fit:contain -- never crops/distorts (per PROD-06)
- No prices anywhere (per PROD-09)
- No text overlay on images -- text is below image in separate area (per PROD-05)
- All 6 products, no empty cards, no duplicates (per PROD-07)
- Supabase image URLs from discovery JSON (per PROD-10)
- Real product page URLs (per PROD-08, already verified)
- Zero style blocks, zero classes, zero display:flex/grid
- Hebrew CTA text "לצפייה במוצר" (View Product)
- Subtle hover on CTA button via inline onmouseover/onmouseout (accent darkened)
- Card background #f9f6f1, border #e8e0d4, border-radius 12px -- matches template design language
- RTL text-align:center on card contents is appropriate since product names are short centered labels

Use exact URLs from hipsterstyle-discovery.json -- copy them character-for-character including the encoded Hebrew paths.
  </action>
  <verify>
    <automated>grep -c "supabase.co/storage/v1/object/public/article-assets/hipsterstyle/product-" claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html | grep -q "6" && grep -c "hipsterstyle.co.il/products/" claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html | grep -q "6" && ! grep -q "Phase 5 will insert" claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html && ! grep -q "display:flex\|display:grid\|display: flex\|display: grid" claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html && echo "PASS"</automated>
  </verify>
  <done>
- 6 product cards rendered with image above, title + CTA below
- All images from Supabase URLs, all links to real product pages
- No stub text remains, no flex/grid, no style blocks, no classes
- Uniform card size via inline-block + percentage width
- No prices displayed, no text overlay on images
  </done>
</task>

</tasks>

<verification>
1. Exactly 6 Supabase image URLs present (one per product)
2. Exactly 6 hipsterstyle.co.il product links present
3. Stub text "Phase 5 will insert product cards here" is gone
4. No display:flex or display:grid anywhere in file
5. No style blocks or class attributes in the products section
6. All product names match discovery JSON
7. No price text in the products section
</verification>

<success_criteria>
- 6 uniform product cards with image-above/text-below layout
- inline-block responsive layout (3 cols desktop, 2 tablet, 1 mobile)
- Supabase image URLs, real product links, no prices
- WordPress-safe: inline styles only, no classes, no style blocks
- Template file updated in place, all other sections untouched
</success_criteria>

<output>
After completion, create `.planning/phases/05-product-card-integration/05-01-SUMMARY.md`
</output>
