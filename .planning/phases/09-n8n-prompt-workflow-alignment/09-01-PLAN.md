---
phase: 09-n8n-prompt-workflow-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - claude-code/Files/Improved_N8N_Prompt-claude-code-2026-03-25.txt
  - claude-code/Files/Improved_N8N_Workflow-claude-code-2026-03-25.json
autonomous: true
requirements: [N8N-01, N8N-02, N8N-03, N8N-04, N8N-05, N8N-06, N8N-07, N8N-08]

must_haves:
  truths:
    - "Prompt TXT contains the exact injection block verbatim from CONTEXT.md"
    - "No extra N8N injection lines exist beyond the specified block"
    - "Zero oritmartin.com references remain in prompt or workflow"
    - "Style block contradiction resolved — no MANDATORY CSS BLOCK section, FORBIDDEN stays authoritative"
    - "Workflow Clean HTML node no longer self-contradicts"
    - "Workflow implements two-phase LLM generation (content then HTML rendering)"
  artifacts:
    - path: "claude-code/Files/Improved_N8N_Prompt-claude-code-2026-03-25.txt"
      provides: "Rebuilt N8N prompt with exact injection block and hipsterstyle branding"
    - path: "claude-code/Files/Improved_N8N_Workflow-claude-code-2026-03-25.json"
      provides: "Updated workflow JSON aligned to new prompt and template"
  key_links:
    - from: "Improved_N8N_Prompt-claude-code-2026-03-25.txt"
      to: "Improved_N8N_Workflow-claude-code-2026-03-25.json"
      via: "Build Final HTML Prompt node embeds prompt instructions matching TXT"
    - from: "Workflow JSON node names"
      to: "Injection block expressions"
      via: "Writing Blog and Preparing Images for HTML node names must match expressions"
---

<objective>
Rebuild the N8N prompt TXT and workflow JSON for hipsterstyle.co.il, fixing all Phase 1 audit contradictions, embedding the exact injection block, and aligning with the new template structure built in Phases 3-8.

Purpose: The prompt and workflow are the production pipeline — they must produce HTML matching the new template, with no contradictions, no oritmartin references, and the exact injection block from CONTEXT.md.
Output: Two files in claude-code/Files/ — the prompt TXT and workflow JSON.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key reference files the executor MUST read before starting each task:

For Task 1 (prompt):
- wp-n8n-html-design-improver/Improved_N8N_Prompt-claude-code-2026-03-25.txt (baseline prompt, 331 lines)
- .planning/phases/09-n8n-prompt-workflow-alignment/09-CONTEXT.md (EXACT injection block)
- .planning/phases/01-baseline-audit-wordpress-risk-assessment/audit/02-prompt-contradiction-report.md (contradictions to fix)
- claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html (new template structure to describe)

For Task 2 (workflow):
- wp-n8n-html-design-improver/Improved_N8N_Workflow-claude-code-2026-03-25.json (baseline workflow, 683 lines)
- .planning/phases/01-baseline-audit-wordpress-risk-assessment/audit/03-workflow-logic-bug-report.md (bugs to fix)
- The TXT file produced by Task 1 (prompt content must match embedded prompt in workflow)

<interfaces>
## HipsterStyle Brand Data (replaces all oritmartin references)

From Phase 2 discovery and template (Phases 3-8):
- Site URL: https://hipsterstyle.co.il/
- Contact page: https://hipsterstyle.co.il/pages/contact
- Logo: https://hipsterstyle.co.il/cdn/shop/files/hipster_logo_black.png
- Brand name (Hebrew): היפסטר
- Brand description: מותג אופנת ילדים ישראלי — סטייל שונה מכולם
- Facebook: https://www.facebook.com/HipsterBabyCollection
- Instagram: https://www.instagram.com/hipster.style/
- Article ID: hs-top (NOT om-top)
- Accent color: #c8a97e (NOT #C4A265)
- Card BG: #f9f6f1
- Border: #e8e0d4
- Body text: #2d2d2d
- Heading: #1a1a1a

## Template Structure (from Phases 3-8 HTML)

Article element: `<article id="hs-top" dir="rtl" lang="he">`
JSON-LD: Two script tags (Article + FAQ) placed INSIDE article after opening tag
Content order: Intro > Summary Card ("במאמר זה") > TOC (details/summary, closed) > 6 H2 sections > Products (inline-block cards) > FAQ (details/summary accordions) > Author section > Floating buttons > </article>
CSS: 100% inline styles, ZERO style blocks, ZERO classes
Hover: onmouseover/onmouseout inline handlers
Accordion: ontoggle with firstElementChild.lastElementChild positional traversal
Product grid: display:inline-block with vertical-align:top (no flex/grid)
Social links: Text labels (Facebook/Instagram), no SVG icons
Floating buttons: HTML entities for icons (triangle up, envelope), position:fixed with clamp()
No WhatsApp button in template

## Exact Injection Block (from CONTEXT.md — NON-NEGOTIABLE)

```
### מוצרים זמינים:
{{ JSON.stringify($json["products"], null, 2) }}

### תוכן הכתבה:
{{ $("Writing Blog").first().json.output }}

חובה! להקפיד לשים במאמר הקישורים החיצוניים וגם הקישורים הפנימיים כמו שקיבלת בתוכן הכתבה.

### תמונות זמינות:
Section image 1 URL: {{ $("Preparing Images for HTML").first().json.images.section_1.url }}
Section image 2 URL: {{ $("Preparing Images for HTML").first().json.images.section_2.url }}
Section image 3 URL: {{ $("Preparing Images for HTML").first().json.images.section_3.url }}
Section image 4 URL: {{ $("Preparing Images for HTML").first().json.images.section_4.url }}
Hero image URL:      {{ $("Preparing Images for HTML").first().json.images.hero.url }}
```

NOTE: The injection block in the TXT file must NOT wrap the image URLs in code fences. The CONTEXT.md specifies them without code fences. The baseline prompt has them wrapped in ```text ... ``` — remove those fences.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild N8N prompt TXT for hipsterstyle</name>
  <files>claude-code/Files/Improved_N8N_Prompt-claude-code-2026-03-25.txt</files>

  <read_first>
    - wp-n8n-html-design-improver/Improved_N8N_Prompt-claude-code-2026-03-25.txt
    - .planning/phases/09-n8n-prompt-workflow-alignment/09-CONTEXT.md
    - .planning/phases/01-baseline-audit-wordpress-risk-assessment/audit/02-prompt-contradiction-report.md
    - claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html
  </read_first>

  <action>
  Rebuild the prompt TXT from scratch using the baseline as structural reference. The output goes to claude-code/Files/Improved_N8N_Prompt-claude-code-2026-03-25.txt (NOT the wp-n8n-html-design-improver copy).

  CRITICAL CHANGES (per N8N-01 through N8N-06 and Phase 1 audit):

  1. INJECTION BLOCK (N8N-01, N8N-02, N8N-03, N8N-04, N8N-05, N8N-06):
     - Place the EXACT injection block from CONTEXT.md verbatim in the prompt
     - The image URLs must NOT be wrapped in code fences (```text ... ```) — the CONTEXT.md block has them bare
     - No extra N8N injection lines anywhere else in the prompt
     - Keep the "ADDITIONAL NON-NEGOTIABLE RULE" section that protects the block (Lines 71-80 of baseline) but update it to match the new block format

  2. FIX CRIT-1 — STYLE BLOCK CONTRADICTION:
     - DELETE the entire "MANDATORY CSS BLOCK" section (baseline Lines 28-44, the <style> block)
     - DELETE the style block instruction at baseline Line 18 ("PLUS one <style> block...")
     - KEEP the FORBIDDEN PATTERNS list entry "Style blocks" (baseline Line 292)
     - KEEP the QA checklist item "No style block exists" (baseline Line 320)
     - The NON-NEGOTIABLE OUTPUT RULES Line 18 should say: "Inline CSS on each element. No style blocks. Use onmouseover, onmouseout, and ontoggle handlers for hover and accordion effects."
     - DELETE the "MANDATORY CLASS ASSIGNMENTS" section (baseline Lines 47-56) — no classes since no style block

  3. FIX CRIT-2 — JSON-LD:
     - The new template (Phases 3-8) DOES include JSON-LD (Article + FAQ) as script tags INSIDE the article, right after opening tag
     - UPDATE the prompt to ALLOW JSON-LD script tags placed immediately after the opening article tag (before any visible content)
     - Remove the prohibition "No JSON-LD block at the end" (baseline Line 26)
     - Add instruction: "Include JSON-LD Article and FAQ schema as script tags immediately after the opening article tag, before any visible content. The author section remains the final VISIBLE section."
     - Update QA checklist: "JSON-LD Article and FAQ schema present inside article" instead of no JSON-LD

  4. REPLACE ALL oritmartin.com REFERENCES:
     - Title: "HIPSTERSTYLE ARTICLE SYSTEM — IMPROVED N8N PROMPT"
     - Role: "HipsterStyle's WordPress article pipeline"
     - Site URL: https://hipsterstyle.co.il/
     - Contact page: https://hipsterstyle.co.il/pages/contact
     - Logo: https://hipsterstyle.co.il/cdn/shop/files/hipster_logo_black.png
     - Brand name: היפסטר (Hebrew), HipsterStyle (English)
     - Brand description: מותג אופנת ילדים ישראלי — סטייל שונה מכולם
     - Facebook: https://www.facebook.com/HipsterBabyCollection
     - Instagram: https://www.instagram.com/hipster.style/
     - Article ID: hs-top (not om-top)
     - Accent color: #c8a97e (not #C4A265)
     - REMOVE: phone, email, about page, gallery page references (hipsterstyle is a Shopify store, no physical gallery or personal phone)
     - REMOVE: author portrait Supabase URL (hipsterstyle uses logo, not portrait)
     - REMOVE: "Orit Martin" name, "Jerusalem/Har Nof" bio
     - REMOVE: WhatsApp, LinkedIn, YouTube — not verified for hipsterstyle

  5. UPDATE TEMPLATE STRUCTURE DESCRIPTION to match Phases 3-8:
     - Article ID is hs-top
     - No classes, no style blocks — pure inline styles
     - Accordion indicator uses float:left positioning (not display:flex)
     - Product grid uses display:inline-block with vertical-align:top
     - Social links use text labels, not SVG icons
     - Floating buttons use HTML entities (triangle up, envelope), not SVG
     - Author section shows logo image, brand name, description, social links, and CTA button
     - No phone/email buttons in author section (it's a brand, not personal)
     - Remove CTA banner instructions (not part of hipsterstyle template)
     - Remove "Inline CTA BANNERS" section entirely — template has no mid-article CTAs

  6. FIX HIGH-1 — WordPress-unsafe CSS instructions:
     - Replace all "display:flex with justify-content:space-between" instructions with float-based approach
     - Accordion indicator: "Use float:left on the + span, not display:flex on summary"
     - Social link layout: text-based inline links, not flex containers
     - Document that `display` property is NOT on wp_kses_post() safe_style_css whitelist

  7. UPDATE PURPOSE SECTION:
     - From Kabbalah/Sefirot/spiritual art to kids fashion/styling
     - Purpose: "Create a polished Hebrew article about kids styling, children's fashion trends, or the injected topic in a way that naturally supports HipsterStyle's online store."

  8. VERIFY after writing:
     - grep for "oritmartin" — must return 0 matches
     - grep for "orit" (case insensitive) — must return 0 matches
     - grep for "om-top" — must return 0 matches
     - grep for "C4A265" — must return 0 matches
     - grep for "<style>" — must return 0 matches (style blocks forbidden)
     - grep for "class=" — must return 0 matches (no classes without style block)
     - Confirm the exact injection block appears verbatim
     - Confirm no code fences (```) wrap the image URLs in the injection block
  </action>

  <verify>
    <automated>
    bash -c "FILE='claude-code/Files/Improved_N8N_Prompt-claude-code-2026-03-25.txt' && echo '--- Checking file exists ---' && test -f \"$FILE\" && echo 'OK: File exists' || (echo 'FAIL: File missing' && exit 1) && echo '--- Checking no oritmartin refs ---' && if grep -i 'oritmartin\|orit.martin\|orit_martin' \"$FILE\"; then echo 'FAIL: oritmartin references found'; exit 1; else echo 'OK: No oritmartin refs'; fi && echo '--- Checking no om-top ---' && if grep 'om-top' \"$FILE\"; then echo 'FAIL: om-top found'; exit 1; else echo 'OK: No om-top'; fi && echo '--- Checking no style block instruction ---' && if grep -i '<style>' \"$FILE\"; then echo 'FAIL: style block found'; exit 1; else echo 'OK: No style blocks'; fi && echo '--- Checking no class= instruction ---' && if grep 'class=\"' \"$FILE\"; then echo 'FAIL: class attributes found'; exit 1; else echo 'OK: No class attributes'; fi && echo '--- Checking injection block present ---' && grep -c 'JSON.stringify.*products' \"$FILE\" && grep -c 'Writing Blog.*first.*json.output' \"$FILE\" && grep -c 'Preparing Images for HTML.*section_1' \"$FILE\" && echo '--- Checking no code fences around image URLs ---' && if grep -B1 'Section image 1 URL' \"$FILE\" | grep '^\`\`\`'; then echo 'FAIL: code fences found around image URLs'; exit 1; else echo 'OK: No code fences'; fi && echo '--- Checking hipsterstyle present ---' && grep -c 'hipsterstyle' \"$FILE\" && echo 'ALL CHECKS PASSED'"
    </automated>
  </verify>

  <done>
  - Prompt TXT exists at claude-code/Files/Improved_N8N_Prompt-claude-code-2026-03-25.txt
  - Contains the EXACT injection block from CONTEXT.md verbatim (no code fences on image URLs)
  - Zero oritmartin.com / orit / om-top / C4A265 references remain
  - No style block instructions, no class assignments
  - JSON-LD allowed (placed inside article after opening tag)
  - All template structure descriptions match Phases 3-8 HTML
  - WordPress-unsafe CSS patterns (display:flex) replaced with safe alternatives
  - Brand-specific data updated to hipsterstyle.co.il
  </done>
</task>

<task type="auto">
  <name>Task 2: Update N8N workflow JSON aligned to new prompt and template</name>
  <files>claude-code/Files/Improved_N8N_Workflow-claude-code-2026-03-25.json</files>

  <read_first>
    - wp-n8n-html-design-improver/Improved_N8N_Workflow-claude-code-2026-03-25.json
    - .planning/phases/01-baseline-audit-wordpress-risk-assessment/audit/03-workflow-logic-bug-report.md
    - claude-code/Files/Improved_N8N_Prompt-claude-code-2026-03-25.txt (Task 1 output)
  </read_first>

  <action>
  Rebuild the workflow JSON from the baseline, fixing all Phase 1 audit bugs and aligning to the new prompt and template. Output goes to claude-code/Files/Improved_N8N_Workflow-claude-code-2026-03-25.json.

  CRITICAL FIXES:

  1. FIX BUG-1 — Clean HTML Node Self-Contradiction (N8N-07):
     Replace the preserve-then-reject logic with unconditional style stripping. The new Clean HTML node code must:
     ```javascript
     // Strip ALL style blocks unconditionally (WordPress strips them anyway)
     html = html.replace(/<style[\s\S]*?<\/style>/gi, '');
     html = html.replace(/<!--[\s\S]*?-->/g, '');
     html = html.replace(/<script[\s\S]*?<\/script>/gi, '');
     ```
     WAIT — JSON-LD uses script tags. The template has JSON-LD as `<script type="application/ld+json">`. The Clean HTML node must PRESERVE ld+json scripts while stripping other scripts. Update regex:
     ```javascript
     // Strip all script tags EXCEPT application/ld+json (JSON-LD schema)
     html = html.replace(/<script(?![^>]*type\s*=\s*["']application\/ld\+json["'])[^>]*>[\s\S]*?<\/script>/gi, '');
     ```
     Then the validation check should NOT throw on ld+json scripts. Remove or update the old forbidden markup check:
     ```javascript
     // Validate: no style blocks, no comments, no non-ld+json scripts
     if (/<style\b/i.test(html)) throw new Error('Style block found in output.');
     if (/<!--/.test(html)) throw new Error('HTML comment found in output.');
     // ld+json scripts are allowed, only check for non-ld+json scripts
     if (/<script(?![^>]*type\s*=\s*["']application\/ld\+json["'])/i.test(html)) throw new Error('Non-LD+JSON script found in output.');
     ```
     Keep the article start/end validation.
     Remove the em-dash replacement (cosmetic, unnecessary).

  2. FIX BUG-2 — Build Final HTML Prompt Node (N8N-07):
     The embedded prompt in this node must match the new TXT file content from Task 1. Replace the entire jsCode string to:
     - Remove the "MUST include one <style> block" instruction
     - Remove all class assignment instructions
     - Keep "Inline CSS only" as authoritative
     - Use hs-top not om-top
     - Use hipsterstyle brand data
     - Include the exact injection block (same as TXT)
     - Describe template structure matching Phases 3-8

  3. FIX BUG-4 — WordPress Publish Content Escaping (N8N-07):
     Replace the Publish to WordPress HTTP Request node's raw JSON body interpolation with a safe approach. Change the node to use N8N expression body mapping instead of raw string interpolation:
     ```json
     "sendBody": true,
     "specifyBody": "keypair",
     "bodyParameters": {
       "parameters": [
         { "name": "title", "value": "={{ $json.title }}" },
         { "name": "content", "value": "={{ $json.content }}" },
         { "name": "status", "value": "={{ $json.status }}" }
       ]
     }
     ```
     This lets N8N handle JSON encoding properly instead of manual string interpolation.

  4. REPLACE ALL oritmartin REFERENCES (N8N-07):
     Update every node that contains oritmartin data:

     a) Workflow root name: "Improved Content Pipeline - HipsterStyle - claude-code - 2026-03-25"

     b) Normalize Input node verifiedLinks:
        - siteUrl: 'https://hipsterstyle.co.il/'
        - businessName: 'היפסטר — מותג אופנת ילדים ישראלי'
        - businessDescription: 'סטייל שונה מכולם. בגדי ילדים מעוצבים ואיכותיים.'
        - aboutUrl: Remove (Shopify store, no about page in same format)
        - galleryUrl: Remove (not applicable)
        - phone: Remove (not personal)
        - email: Remove (not personal)
        - facebookUrl: 'https://www.facebook.com/HipsterBabyCollection'
        - Add instagramUrl: 'https://www.instagram.com/hipster.style/'
        - contactUrl: 'https://hipsterstyle.co.il/pages/contact'

     c) Build Blog Brief node: Update Hebrew prompt to reference hipsterstyle, kids fashion, not Kabbalah art

     d) Notify Slack (Success): Replace "Orit Martin" with dynamic brand name from Normalize Input

     e) Error Handler (Slack): Same replacement

     f) Workflow meta templateId: "improved-content-pipeline-hipsterstyle-claude-code-2026-03-25"

     g) Tags: Replace "orit-martin" tag with "hipsterstyle"

  5. PRESERVE TWO-PHASE LLM GENERATION (N8N-08):
     The workflow pipeline must maintain:
     Phase 1: Write Blog Draft (LLM) -> Writing Blog (validates output)
     Phase 2: Preparing Images for HTML -> Build Final HTML Prompt -> Render Final HTML (LLM) -> Clean HTML
     Do NOT merge these into a single LLM call.

  6. PRESERVE NODE NAMES that the injection block references:
     - "Writing Blog" — must keep this exact name (injection block uses it)
     - "Preparing Images for HTML" — must keep this exact name (injection block uses it)

  7. CONNECT Error Handler (BUG-5 from audit):
     Set errorWorkflow in settings to reference error handling. If N8N does not support self-referencing errorWorkflow without a workflow ID, at minimum add an onError handler pattern to the critical nodes (Clean HTML, Publish to WordPress).

  8. VERIFY after writing:
     - Parse JSON to confirm valid syntax
     - grep for "oritmartin" — must return 0 matches
     - grep for "orit" (case insensitive) — must return 0 matches
     - grep for "om-top" — must return 0 matches
     - Confirm "Writing Blog" and "Preparing Images for HTML" node names preserved
     - Confirm Clean HTML node has no preserve-then-reject contradiction
  </action>

  <verify>
    <automated>
    bash -c "FILE='claude-code/Files/Improved_N8N_Workflow-claude-code-2026-03-25.json' && echo '--- Checking file exists ---' && test -f \"$FILE\" && echo 'OK: File exists' || (echo 'FAIL: File missing' && exit 1) && echo '--- Checking valid JSON ---' && node -e \"JSON.parse(require('fs').readFileSync('$FILE','utf8'));console.log('OK: Valid JSON')\" && echo '--- Checking no oritmartin refs ---' && if grep -i 'oritmartin\|orit.martin\|orit_martin\|orit-martin' \"$FILE\"; then echo 'FAIL: oritmartin references found'; exit 1; else echo 'OK: No oritmartin refs'; fi && echo '--- Checking no om-top ---' && if grep 'om-top' \"$FILE\"; then echo 'FAIL: om-top found'; exit 1; else echo 'OK: No om-top'; fi && echo '--- Checking Writing Blog node preserved ---' && grep -c '\"Writing Blog\"' \"$FILE\" && echo 'OK: Writing Blog node exists' && echo '--- Checking Preparing Images node preserved ---' && grep -c '\"Preparing Images for HTML\"' \"$FILE\" && echo 'OK: Preparing Images node exists' && echo '--- Checking hipsterstyle present ---' && grep -c 'hipsterstyle' \"$FILE\" && echo '--- Checking no preserve-then-reject in Clean HTML ---' && if grep 'om-top' \"$FILE\"; then echo 'FAIL: om-top still in Clean HTML regex'; exit 1; else echo 'OK: Clean HTML fixed'; fi && echo 'ALL CHECKS PASSED'"
    </automated>
  </verify>

  <done>
  - Workflow JSON exists at claude-code/Files/Improved_N8N_Workflow-claude-code-2026-03-25.json
  - Valid JSON (parseable)
  - Clean HTML node strips ALL style blocks, preserves ld+json scripts, no self-contradiction
  - Build Final HTML Prompt node matches new TXT prompt (no style block, no classes, hipsterstyle brand)
  - WordPress Publish node uses safe content encoding (not raw string interpolation)
  - Zero oritmartin/orit/om-top references remain
  - Node names "Writing Blog" and "Preparing Images for HTML" preserved (injection block depends on them)
  - Two-phase LLM generation pattern maintained
  - Slack notifications use dynamic brand name
  - Workflow metadata updated to hipsterstyle
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run comprehensive cross-file validation:

1. Injection block consistency: The exact injection block must appear identically in both the TXT file and the embedded prompt in the Build Final HTML Prompt node of the JSON workflow.

2. Brand consistency: grep both files for any remaining oritmartin/orit/om-top/C4A265 references — must find zero.

3. Template alignment: The prompt instructions must describe the actual HTML structure from the Phase 3-8 template (hs-top ID, inline CSS only, no classes, float-based accordion indicators, inline-block product grid, text social labels, HTML entity floating buttons).

4. No contradictions: The prompt must not simultaneously mandate and forbid any pattern (style blocks, JSON-LD, etc.).
</verification>

<success_criteria>
- Both files exist in claude-code/Files/ with correct dated names
- Prompt TXT contains exact injection block from CONTEXT.md (no code fences on image URLs)
- Zero oritmartin/orit/om-top references across both files
- Style block contradiction eliminated (CRIT-1 from audit)
- Clean HTML node self-contradiction eliminated (BUG-1 from audit)
- WordPress Publish escaping fixed (BUG-4 from audit)
- Two-phase LLM generation preserved in workflow
- All N8N-01 through N8N-08 requirements addressed
</success_criteria>

<output>
After completion, create `.planning/phases/09-n8n-prompt-workflow-alignment/09-01-SUMMARY.md`
</output>
