---
phase: 08-seo-schema-hover-states-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-03
  - HOVER-05
must_haves:
  truths:
    - "JSON-LD Article schema is embedded inside the article element"
    - "JSON-LD FAQ schema covers all 6 FAQ questions and answers"
    - "Heading hierarchy is H2-only (no H1 — WordPress supplies H1 via post title)"
    - "All 27 hover elements across TOC, FAQ, CTA, social, floating buttons use inline JS handlers and remain readable"
    - "Zero style blocks, zero classes, zero display property in final output"
  artifacts:
    - path: "claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html"
      provides: "Complete template with JSON-LD schema and audited hover states"
      contains: "application/ld+json"
  key_links:
    - from: "script type=application/ld+json (Article)"
      to: "article element headings and metadata"
      via: "headline, author, publisher properties"
      pattern: "application/ld\\+json"
    - from: "script type=application/ld+json (FAQ)"
      to: "FAQ details/summary elements"
      via: "mainEntity array with Question/Answer pairs"
      pattern: "FAQPage"
    - from: "onmouseover/onmouseout handlers"
      to: "inline style properties"
      via: "this.style assignments"
      pattern: "onmouseover.*onmouseout"
---

<objective>
Add JSON-LD Article and FAQ schema markup to the template and audit all hover states for WordPress safety.

Purpose: SEO schema gives search engines structured data for rich snippets (Article + FAQ). The hover audit ensures all interactive states remain visible, readable, and WordPress-safe across all template sections.

Output: Updated HTML template with two JSON-LD script blocks inside the article element and verified hover states.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html

<interfaces>
<!-- Current template structure (226 lines) -->
<!-- Line 1: <article id="hs-top" dir="rtl" lang="he" ...> -->
<!-- Line 226: </article> -->

Heading hierarchy (all H2, no H1 — WordPress post title is H1):
- H2: כותרת ראשית ראשונה (line 80, id=section-1)
- H2: כותרת ראשית שנייה (line 85, id=section-2)
- H2: כותרת ראשית שלישית (line 90, id=section-3)
- H2: כותרת ראשית רביעית (line 95, id=section-4)
- H2: כותרת ראשית חמישית (line 100, id=section-5)
- H2: כותרת ראשית שישית (line 105, id=section-6)
- H2: מוצרים מומלצים (line 110, id=products)
- H2: שאלות נפוצות (line 170, id=faq)
- H2: אודות הכותב (line 210, id=author)

FAQ Q&A pairs (6 items, lines 171-206):
1. Q: "איך לבחור מידה נכונה לילדים שגדלים מהר?" A: "מומלץ למדוד את הילד לפני כל רכישה ולבחור בגדים עם מעט מרווח לגדילה..."
2. Q: "האם הבגדים שלכם מתאימים לעונות השנה השונות?" A: "בהחלט. הקולקציות שלנו מתחלפות..."
3. Q: "איך משלבים צבעים בלבוש ילדים בלי להגזים?" A: "הכלל הפשוט הוא לבחור בסיס ניטרלי..."
4. Q: "מה ההבדל בין בגדי יומיום לבגדי אירועים לילדים?" A: "בגדי יומיום מתמקדים בנוחות..."
5. Q: "האם יש משלוחים לכל הארץ?" A: "כן, אנחנו מציעים משלוחים..."
6. Q: "איך מטפלים בבגדים כדי שיחזיקו לאורך זמן?" A: "כדאי לכבס בטמפרטורה נמוכה..."

Hover elements inventory (27 total):
- TOC summary header: 1 (line 47) — color shift to #c8a97e
- TOC nav links: 9 (lines 50-74) — color #c8a97e + underline
- Product CTA "לצפייה במוצר" buttons: 6 (lines 117-162) — bg #b89968
- FAQ summary headers: 6 (lines 172-202) — color shift to #c8a97e
- Social links (Facebook/Instagram): 2 (lines 216-217) — brand-color fill
- Author CTA "בקרו באתר שלנו": 1 (line 219) — bg #b89968 + translateY + boxShadow
- Floating Back to Top: 1 (line 223) — bg #b89968 + scale(1.1)
- Floating Contact: 1 (line 224) — bg swap to #c8a97e

Existing constraints (MUST maintain):
- Zero <style> blocks
- Zero class attributes
- Zero display CSS property
- All inline style attributes only
- onmouseover/onmouseout for hover states (WordPress strips <style>-based :hover)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add JSON-LD Article and FAQ schema scripts inside article element</name>
  <files>claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html</files>
  <action>
    Read the full template file. Insert two script type="application/ld+json" blocks immediately after the opening article tag (line 1) and before the first paragraph (line 4). Place Article schema first, then FAQ schema. Use two separate script tags for clarity.

    **Article schema (per SEO-01):**
    ```json
    {
      "@context": "https://schema.org",
      "@type": "Article",
      "headline": "טיפים לסטיילינג מושלם לילדים — המדריך המלא",
      "description": "מדריך מקיף לבחירת בגדים מעוצבים ונוחים לילדים, כולל טיפים לשילוב צבעים, טרנדים עדכניים ומוצרים מומלצים",
      "author": {
        "@type": "Organization",
        "name": "היפסטר",
        "url": "https://hipsterstyle.co.il/"
      },
      "publisher": {
        "@type": "Organization",
        "name": "היפסטר",
        "url": "https://hipsterstyle.co.il/",
        "logo": {
          "@type": "ImageObject",
          "url": "https://hipsterstyle.co.il/cdn/shop/files/hipster_logo_black.png"
        }
      },
      "datePublished": "2026-03-25",
      "dateModified": "2026-03-25",
      "inLanguage": "he",
      "mainEntityOfPage": {
        "@type": "WebPage",
        "@id": "https://hipsterstyle.co.il/"
      }
    }
    ```

    **FAQ schema (per SEO-02):**
    ```json
    {
      "@context": "https://schema.org",
      "@type": "FAQPage",
      "mainEntity": [
        {
          "@type": "Question",
          "name": "איך לבחור מידה נכונה לילדים שגדלים מהר?",
          "acceptedAnswer": {
            "@type": "Answer",
            "text": "מומלץ למדוד את הילד לפני כל רכישה ולבחור בגדים עם מעט מרווח לגדילה. שימו לב לטבלאות המידות באתר ואל תהססו לפנות לשירות הלקוחות שלנו לייעוץ אישי."
          }
        },
        ... (all 6 Q&A pairs from the FAQ section)
      ]
    }
    ```

    CRITICAL constraints:
    - Script tags MUST be inside the article element (not in head or outside article)
    - Use exact question and answer text from the existing FAQ section (lines 171-206)
    - No trailing commas in JSON
    - Verify JSON validity before saving

    **Heading hierarchy verification (per SEO-03):**
    After inserting schema, scan all heading tags. Confirm:
    - No H1 tags exist (WordPress post title provides H1)
    - All section headings are H2 (9 total)
    - No heading level skips (no H4 without H3, etc.)
    - Document in SUMMARY that hierarchy is clean: 0 x H1, 9 x H2, 0 x H3
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html','utf8');const schemas=h.match(/type=\"application\/ld\+json\">([\s\S]*?)<\/script>/g);if(!schemas||schemas.length!==2){process.exit(1)}schemas.forEach((s,i)=>{const j=s.replace(/type=\"application\/ld\+json\">/,'').replace(/<\/script>/,'');JSON.parse(j);});const art=schemas[0].includes('Article');const faq=schemas[1].includes('FAQPage');if(!art||!faq)process.exit(1);const artIdx=h.indexOf('<article');const scriptIdx=h.indexOf('application/ld+json');if(scriptIdx<artIdx)process.exit(1);console.log('PASS: 2 valid JSON-LD scripts inside article (Article + FAQPage)')"</automated>
  </verify>
  <done>
    - Two script type="application/ld+json" blocks exist inside the article element
    - Article schema has headline, author (Organization), publisher with logo, dates, inLanguage: he
    - FAQ schema has all 6 Q&A pairs with exact text from template
    - Both JSON blocks parse without error
    - Heading hierarchy confirmed: 0xH1, 9xH2, no skips
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit all hover states for WordPress safety and readability</name>
  <files>claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html</files>
  <action>
    After Task 1 completes, read the updated template and perform a systematic audit of all 27 hover elements (per HOVER-05). For each element, verify these WordPress safety rules:

    **Audit checklist per hover element:**
    1. Uses onmouseover/onmouseout inline JS (NOT CSS :hover) -- WordPress strips style blocks
    2. onmouseout restores EXACT original style values (no "stuck" hover state)
    3. Hover color remains readable against its background (contrast check)
    4. No hover state hides text (color matching background = hidden text = SEO penalty)
    5. No hover state uses display property
    6. No hover state uses class manipulation

    **Expected inventory to verify:**

    | Section | Count | Hover Pattern | Expected |
    |---------|-------|--------------|----------|
    | TOC summary | 1 | color #1a1a1a -> #c8a97e | OK |
    | TOC links | 9 | color #4a4a4a -> #c8a97e + underline | OK |
    | Product CTAs | 6 | bg #c8a97e -> #b89968 | OK |
    | FAQ summaries | 6 | color #1a1a1a -> #c8a97e | OK |
    | Social links | 2 | fill brand-color + white text | Check contrast |
    | Author CTA | 1 | bg #b89968 + translateY + boxShadow | OK |
    | Float Back to Top | 1 | bg #b89968 + scale(1.1) | OK |
    | Float Contact | 1 | bg #1a1a1a -> #c8a97e | OK |

    **Specific checks:**
    - Social link hover: white text (#ffffff) on FB blue (#1877F2) and IG pink (#E4405F) — GOOD contrast
    - TOC/FAQ hover: gold (#c8a97e) on white (#f9f6f1) bg — verify sufficient contrast (ratio ~2.9:1 for large text, acceptable for decorative emphasis on already-readable text)
    - Product CTA hover: white text on slightly darker gold — GOOD contrast
    - Floating buttons hover: white on gold or scale effect — GOOD

    **If any issues found:** Fix them inline. Likely issues:
    - onmouseout not restoring all changed properties
    - Missing onmouseout entirely on an element
    - Color on hover too similar to background

    **Final validation:**
    - Count all onmouseover occurrences — must equal onmouseout count
    - Grep for "display:" — must return ZERO matches in style attributes
    - Grep for "class=" — must return ZERO matches
    - Grep for "<style" — must return ZERO matches
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('claude-code/Files/Improved_HTML_Template-claude-code-2026-03-25.html','utf8');const ov=(h.match(/onmouseover=/g)||[]).length;const ou=(h.match(/onmouseout=/g)||[]).length;if(ov!==ou){console.error('FAIL: onmouseover('+ov+') !== onmouseout('+ou+')');process.exit(1)}if(ov<27){console.error('FAIL: only '+ov+' hover elements, expected >=27');process.exit(1)}if(/<style/i.test(h)){console.error('FAIL: style block found');process.exit(1)}if(/\sclass=/i.test(h)){console.error('FAIL: class attribute found');process.exit(1)}const styleAttrs=h.match(/style=\"[^\"]*display[^\"]*\"/gi);if(styleAttrs){console.error('FAIL: display property in style attrs');process.exit(1)}console.log('PASS: '+ov+' hover pairs, zero style blocks, zero classes, zero display')"</automated>
  </verify>
  <done>
    - All 27 hover elements verified: onmouseover count equals onmouseout count
    - Every onmouseout restores original property values
    - No hover state hides text or breaks layout
    - Zero style blocks, zero class attributes, zero display property
    - Template remains fully WordPress-safe
  </done>
</task>

</tasks>

<verification>
1. Template contains exactly 2 JSON-LD script blocks (Article + FAQPage) inside the article element
2. Both JSON blocks parse without error
3. FAQ schema contains all 6 Q&A pairs with exact Hebrew text
4. Heading hierarchy: 0xH1, 9xH2, no skips
5. 27+ hover elements all have matched onmouseover/onmouseout pairs
6. Zero style blocks, zero classes, zero display property in entire template
</verification>

<success_criteria>
- JSON-LD Article schema with proper headline, author (Organization), publisher, dates, inLanguage
- JSON-LD FAQ schema with all 6 question/answer pairs matching template text
- Both scripts are inside the article element (not in head)
- Heading hierarchy clean for SEO crawlers
- All hover states remain readable and WordPress-safe via inline JS handlers
- Template constraints (no style blocks, no classes, no display) maintained
</success_criteria>

<output>
After completion, create `.planning/phases/08-seo-schema-hover-states-polish/08-01-SUMMARY.md`
</output>
